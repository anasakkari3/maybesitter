/**
 * Incremental replanning (#524, slice 1): the impact closure. Steps 3–8 of
 * the issue's algorithm — freeze, re-solve, validate, diff, escalate — are
 * the deliberate follow-up slice and are not here.
 */
export * from './impactClosure';
