# MaybeSitter Round 3 — grounded context

Branch: `design/astra-r2-polish`. No merge or push.

1. **Starting SHA:** `4465a48a2fbed0e13973a6589f5fb48453443ed9`.
2. **Final SHA:** recorded in the delivered comparison manifest and handoff. Both revisions are pinned there.
3. **Today:** Previously, one primary action, a plan-exists row, grouped tasks and upcoming records. Now, the same primary action leads; the plan previews up to four ordered slots. Actual completions have checks. Accepted plans identify the next open slot as “Next in plan,” never as work happening now. Proposed/edited plans remain explicitly suggested. Previewed tasks leave the secondary groups; overflow remains reachable. A single completion line replaces the open count only when meaningful. One explicitly important tomorrow deadline replaces its duplicate upcoming row. Empty and light days retain their simpler composition.
4. **Calendar:** The week strip, selected-day schedule and busy-source behavior remain. One important deadline on another displayed day adds weekly context; it never repeats a record already on the selected day. No external calendar coverage is inferred.
5. **Capture:** The composer, examples, voice, paste/share, API call and routing stay intact. The real response’s item title now appears as “Here’s what I understood” when clarification is needed. This is the existing immediate response, not a new timed intermediate screen or chat history.
6. **Clarification:** A concise understanding block precedes the existing question. Remaining-question copy replaces the administrative position treatment. Quick answers, custom text, skip behavior, unknown-key fallback and server-authoritative updates are preserved.
7. **Review:** “For confirmation” and a short explanation introduce the existing structured facts. The black selection stripe is removed. An outlined checked teal box denotes selection; dashed proposal boundaries and the unchanged unsaved notice denote a proposal; the filled button remains the explicit write action. Nothing is labelled a confirmed appointment or fixed event. At accessibility sizes, the heading moves into the existing task header and the actions follow the facts inside the scroller; Back stays pinned.
8. **Saved:** A larger, clear teal success circle and contrasting check restore the completion moment. Real persisted receipts, partial failures, Undo timing, scrolling and actions remain unchanged. No glow was added.
9. **Adopted reference ideas:** Tangible plan order and times, one factual completion line, a bounded upcoming section, a deadline reminder, visible understanding before a question, structured confirmation and a stronger success mark.
10. **Rejected reference ideas:** Metric grids, streaks, completion percentages, decorative greetings/quotes, emoji badges, full chatbot transcripts, typing delays, heatmaps, presumed free-time gaps, “lighter than usual,” and automatic workload-distribution advice.
11. **Why:** These either duplicated the existing decision, consumed space without helping it, implied unsupported history/calendar coverage, or blurred an unsaved proposal with a fact. AX5 review’s redundant body heading/subtitle were also removed from that layout because they displaced the details.
12. **Data limits:** Existing authenticated mobile APIs only; no backend or auth changes. Progress excludes dropped records and completions on another local date. Deadline context requires `due_by`, a real due date and `user_explicit` high priority. Plan preview joins current records and omits missing/dropped items. The client has no trustworthy postponement count, historical workload baseline, goal-time coverage or comprehensive calendar availability for the richer suggested claims. AI remains off in the local evidence account, exercising the real server rule engine. Test records were explicitly confirmed through Capture APIs in the disposable account; no production seed or runtime fake data was introduced.
13. **Accessibility / RTL / Dynamic Type:** Arabic light/dark, English, Hebrew, xxxLarge and AX5 captured on the same iPhone 17 Pro / iOS 26.5. Existing native RTL alignment, line-height scaling, safe areas and uncapped text remain. New copy exists in all three locales; Hebrew retains its existing machine-translated status. AX5 Today’s reminder was reached by scrolling and opened real Details. Review actions are reachable after the facts; a regression test drives confirmation through that layout. This is simulator evidence, not a physical-device, Android/TalkBack or exhaustive VoiceOver certification.
14. **Matched gallery:** [HTML comparison](../../../../outputs/astra-r3-context/index.html), with exactly 44 raw PNGs in the adjacent `screenshots/` directory, two per state. BEFORE left, AFTER right, equal 1206 × 2622 source dimensions, no added UI overlays. All eight scenario snapshot hashes and text-size settings match. Exact state pairs: **01–05, 07–08, 13–21**. Exceptions: **06** compares the bottom of Today using the same records, but the new section has no identical baseline and changes scroll geometry; **09–12 and 22** use the same input/answers but the real Capture API generates new IDs/timestamps. Pair 09 and 10 share the same immediate response; 03 and 04 share the same busy/proposed-plan state. Pair 14 means a populated commitment week, not connected external busy events. All 22 initial baseline captures preceded product edits; pair 06 was subsequently reframed from an isolated, unchanged checkout at the same baseline SHA.
15. **Automated validation:** Mobile typecheck passed. Full Jest: **199 suites, 2,686 tests, 1 snapshot passed** using `--runInBand --forceExit`. Relevant ESLint: **0 errors, 0 warnings**. The full suite includes RTL, localization parity, native text scaling, design/token and screen-shell regressions, API schema fixtures, capture mutations and plan semantics. New tests cover completion dates, exclusion of unsupported insights, proposal/accepted distinctions, deleted records, preview limits/deduplication, one-primary precedence and accessible Review confirmation. The baseline itself retained test-runner timers; the final run explicitly exits after completed assertions. AST-only Graphify update completed; its existing unrelated parser warning remains in the log.
16. **Regressions / limitations:** No product regression found in the exercised flows. Early evidence attempts had automation failures, retained in logs. At AX5, Maestro cannot reliably reach Capture’s analyze control even after keyboard dismissal; native accessibility activation works, as on the baseline. This existing touch/automation reachability limitation is not certified fixed. An interrupted supplemental AX5 Calendar attempt was rerun separately. Review’s fixed large action area was a density problem found during this iteration and was corrected before handoff. Ordinary simulator flow confirmed Capture → clarification → Review → Confirm → Saved and real proposed-plan acceptance. Failed test-harness attempts (fixture imports and missing endpoint mocks) were corrected; the final full run is green.

**Critical conclusion:** The useful gain is specific: users can see the real plan order, distinguish proposed from accepted work, see actual progress, and notice an important deadline without another task-list duplicate. Clarification and saving communicate their state more clearly. This adds decision support from existing records; it does not claim new personal intelligence. Sections without supporting data remain absent, and additional density that displaced AX5 review facts was removed.

The named design advisory skills were unavailable locally. Repository design/accessibility contracts, the installed React guidance, automated tests and direct simulator comparison were used. Product changes are confined to the requested native surfaces and their tests/copy.
