// Temporary: proves CI goes red on a type error (UC-1.0d #143 acceptance).
// This branch is not for merging and is deleted once the run is recorded.
export const deliberateTypeError: number = 'this is a string, not a number';
